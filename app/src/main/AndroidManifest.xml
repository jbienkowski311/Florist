<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studios.mordor.florist" >
    
    <permission android:name="com.studios.mordor.florist.provider.READWRITE"/>
    <uses-permission android:name="com.studios.mordor.florist.provider.READWRITE"/>
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:authorities="com.studios.mordor.florist.provider"
            android:name="com.studios.mordor.florist.FloristProvider"
            android:exported="true"
            android:readPermission="com.studios.mordor.florist.provider.READWRITE"
            android:writePermission="com.studios.mordor.florist.provider.READWRITE"/>
    </application>

</manifest>
